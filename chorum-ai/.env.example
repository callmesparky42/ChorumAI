# ChorumAI Environment Variables
# Copy this to .env.local and fill in the values

# ===========================================
# REQUIRED - Database
# ===========================================
DATABASE_URL=postgresql://user:password@host:5432/database

# ===========================================
# REQUIRED - Encryption (64 hex chars / 32 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ===========================================
ENCRYPTION_KEY=

# ===========================================
# REQUIRED - Authentication (Supabase)
# ===========================================
# Get these from your Supabase project settings:
# Project Settings > API > Project URL & API Keys
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# SUPABASE DASHBOARD SETUP REQUIRED:
# 1. Authentication > Providers > Enable Google
#    - Add Google Client ID & Secret from console.cloud.google.com
# 2. Authentication > Providers > Enable Email
#    - Configure SMTP for production email delivery
# 3. Authentication > URL Configuration
#    - Set Site URL: https://your-domain.com
#    - Add Redirect URL: https://your-domain.com/auth/callback

# ===========================================
# OPTIONAL - LLM Provider API Keys
# At least one is recommended for chat functionality
# ===========================================
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_AI_API_KEY=
